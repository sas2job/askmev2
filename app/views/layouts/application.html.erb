<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8"/>
    <title>Good questions</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all'%>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <div class="header">
      <div class="container">
        <div class="header-logo">
          <%# Ссылка на главную страницу сайта (root_path) %>
          <h1 class="header-title"><%= link_to "Хорошие вопросы", root_path %></h1>
        </div>

        <div class="header-nav">
          <%= link_to 'Зарегистрироваться', sign_up_path, class: 'button button-light button-small' %>
          <%= link_to 'Войти', log_in_path, class: 'button button-light button-small' %>
        </div>
      </div>
    </div>

    <div class="content">
      <div class="container">

      <%# Выводим сообщение об ошибке или уведомление для пользователя. %>
      <%# Если в объеккте flash есть что-то, выводим содержимое на страницу, %>
      <%# если нет, игнорируем этот блок. %>
      <% if flash.any? %>
        <div class="content-wrapper buffer-both">
          <% flash.each do |name, message| %>
            <%= content_tag :div, message, class: "flash-#{name}" %>
          <% end %>
        </div>
      <% end %>
        <%# Вместо конструкции yield рельсы подставят то, что срендерит %>
        <%# шаблон для нужной страницы, например, users/show %>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
